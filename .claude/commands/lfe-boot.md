---
name: lfe-boot
description: Bootstrap an LFE-compliant session. Reads orientation docs, checks for interrupted sessions, and activates the project persona. Use at the start of any session.
---

> **Generated pointer — not the protocol.** This command exists so `/lfe-boot` dispatches natively in Claude Code. The canonical skill lives at `.agents/skills/lfe-boot/SKILL.md` (LFE-SOURCE).

Read `.agents/skills/lfe-boot/SKILL.md` now and execute its protocol exactly as written — including its position-in-pipeline preconditions and its coordination-file contract. Honor the Skill Invocation Authority rules (LLM_AGENT_GUIDE §8.8): if this skill is agent-dispatched only and was typed out of sequence, refuse and route through the assembly line. Any change to skill behavior belongs in the canonical file, never in this pointer.
